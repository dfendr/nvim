local M = {
    lsp = {

        diagnostic_signs = false,
    },
    ui = {
        winbar_title = false,
        navic = false,

        -- Possible: [double, none, rounded, shadow, single, solid].
        border_style = "rounded",
        -- TODO: Create CMP setting to swap fg/bg on highlight groups
    },
}

return M

